# Manifest Validation Test Fixtures (`tests/fixtures/`)

Fixtures used by unit tests (`src/domain/` manifest validator) to verify strict schema enforcement.

- `valid-*.json`: Valid manifests that MUST pass validation.
- `invalid-*.json`: Malformed/unsupported manifests that MUST fail validation with clear errors.
