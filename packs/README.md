# packs

Official Experience Packs (content only — no executable code).

- `packs/official/` — shipped with the app
- `packs/community/` — community submissions (future)
- `packs/samples/` — sample packs for developers

Packs are declarative: `manifest.json` + assets. See `docs/experiences/`.
