# SDK

The Mischief SDK is the stable, documented interface for third-party developers.

Guides:

- SDK overview and design principles
- Plugin API reference
- Character, experience, rendering, audio, and scheduler APIs
- Testing harnesses

The SDK is versioned with semantic versioning. The Runtime evolves independently while preserving the SDK contract.
