# Plugins

Plugins extend Mischief's capabilities through the SDK.

- Plugin lifecycle
- Plugin manifest reference
- Permissions model
- Sandboxing and crash isolation
- Example plugins in `examples/plugins/`

Plugins must use the SDK only and can never modify Runtime internals.
