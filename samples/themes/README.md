# Mischief Theme Tokens (`samples/themes/`)

Theme JSON files define color palette tokens for the overlay and settings UI.

- `dark-slate.json`: Deep slate dark mode (default theme).
- `vibrant-neon.json`: Vibrant mint/cyan accent theme.
