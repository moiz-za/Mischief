// Hello Plugin entrypoint (skeleton).
// The SDK runtime will call these hooks if present.
export function onInitialize() {
  // called once when the plugin is initialized
}

export function onReady() {
  // called when the plugin is fully loaded and running
}

export function onTick() {
  // called on each runtime tick (if enabled)
}

export function onShutdown() {
  // called before the plugin is unloaded
}
