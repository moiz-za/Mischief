# plugins

Official and community plugins.

- `plugins/official/` — shipped with the app
- `plugins/community/` — community submissions (future)

Plugins use the SDK only. See `docs/plugins/` and `docs/sdk/`.
